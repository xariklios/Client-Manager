# Client Manager — Οδηγός Χρήσης & Δοκιμών

## Σύνδεση

Μεταβείτε στο `http://client-manager.test`. Θα ανακατευθυνθείτε αυτόματα στο `/login`.

| Πεδίο    | Τιμή                           |
|----------|-------------------------------|
| Email    | `charisvaltzis.work@gmail.com` |
| Κωδικός  | `change_me_now` ← **αλλάξτε τον** |

Για να αλλάξετε τον κωδικό, εκτελέστε:
```
php artisan tinker
>>> \App\Models\User::first()->update(['password' => bcrypt('ο-νέος-κωδικός-σας')]);
```

---

## Λειτουργίες & Τρόπος Χρήσης

### Αρχική (`/`)
- Συνολικό ανεξόφλητο ποσό, αριθμός ληξιπρόθεσμων χρεώσεων, 5 πιο πρόσφατοι πελάτες.

### Πελάτες (`/clients`)
- **Λίστα**: αναζήτηση με βάση το όνομα, email ή υπεύθυνο επικοινωνίας. Σελιδοποίηση (20/σελίδα).
- **Προσθήκη**: `/clients/create` — το όνομα είναι υποχρεωτικό, όλα τα υπόλοιπα πεδία προαιρετικά.
- **Προβολή** (`/clients/{id}`): εμφανίζει τα projects, χρεώσεις και προσφορές του πελάτη.
- **Επεξεργασία** (`/clients/{id}/edit`): ενημέρωση στοιχείων, αλλαγή κατάστασης (Ενεργός / Ανενεργός / Αρχειοθετημένος).
- **Διαγραφή**: στη σελίδα επεξεργασίας. Διαγράφει αυτόματα και τα projects, χρεώσεις και προσφορές του.

### Projects
- Δημιουργούνται από τη σελίδα λεπτομερειών πελάτη (`/clients/{id}` → Προσθήκη Project).
- Καταγράφει: όνομα, ημερομηνία έναρξης, ημερομηνία ανανέωσης, κατάσταση, μηνιαία αμοιβή.
- Ανανεώσεις εντός 30 ημερών εμφανίζονται στο email digest επερχόμενων ανανεώσεων.

### Χρεώσεις (`/charges`)
- **Λίστα**: φίλτρο Ανεξόφλητες / Ληξιπρόθεσμες / Όλες. Αναζήτηση με τίτλο ή όνομα πελάτη.
- **Προσθήκη** (`/charges/create`): επιλέξτε πελάτη (προαιρετικά project), ορίστε τίτλο, ποσό, ημερομηνία λήξης.
- **Επεξεργασία** (`/charges/{id}/edit`): αλλαγή οποιουδήποτε πεδίου, ορισμός ημερομηνίας λήξης, διαγραφή χρέωσης.
- **Πληρωμές** (`/charges/{id}/payments/create`): καταγραφή μερικής ή ολικής πληρωμής.
  - Η κατάσταση ενημερώνεται αυτόματα: Ανεξόφλητη → Μερικώς Εξοφλημένη → Εξοφλημένη.
- Ληξιπρόθεσμες γραμμές επισημαίνονται με κόκκινο χρώμα.

### Προσφορές (`/offers`)
- **Λίστα**: φίλτρο Πρόχειρο / Απεσταλμένη / Αποδεκτή / Απορριφθείσα. Αναζήτηση με τίτλο ή όνομα πελάτη.
- **Προσθήκη** (`/offers/create`): επιλογή πελάτη/project, προσθήκη γραμμών (τίτλος, περιγραφή, ποσότητα, τιμή μονάδας). Το σύνολο υπολογίζεται σε πραγματικό χρόνο.
- **Επεξεργασία** (`/offers/{id}/edit`): ενημέρωση γραμμών, αλλαγή κατάστασης (Πρόχειρο → Απεσταλμένη → Αποδεκτή / Απορριφθείσα), καταγραφή ημερομηνίας αποστολής.
- **Λήψη PDF**: κουμπί "↓ Download PDF" στη σελίδα επεξεργασίας — παράγει έτοιμο αρχείο για αποστολή στον πελάτη.

### Αρχείο Email (`/email-logs`)
- Μόνο ανάγνωση. Καταγράφει κάθε αυτόματο digest που εστάλη (ή γράφτηκε στο `laravel.log` κατά την ανάπτυξη).

---

## Αυτόματες Υπενθυμίσεις

Δύο εντολές Artisan, με αυτόματο χρονοδιάγραμμα:

| Εντολή                      | Χρονοδιάγραμμα       | Τι κάνει                                                   |
|-----------------------------|----------------------|------------------------------------------------------------|
| `reminders:overdue`         | Καθημερινά 08:00     | Αποστέλλει digest με όλες τις ανεξόφλητες & ληξιπρόθεσμες χρεώσεις |
| `reminders:renewals`        | Κάθε Δευτέρα 08:00   | Αποστέλλει projects με ανανέωση εντός ≤ 30 ημερών          |

### Χειροκίνητη εκτέλεση (για δοκιμές)

```bash
php artisan reminders:overdue
php artisan reminders:renewals
php artisan reminders:renewals --days=90
```

Κατά την ανάπτυξη, το `MAIL_MAILER=log` στο `.env` σημαίνει ότι τα emails **γράφονται στο `storage/logs/laravel.log`** αντί να αποστέλλονται. Αναζητήστε `To:` σε αυτό το αρχείο για να δείτε την έξοδο.

Για πραγματική αποστολή μέσω SMTP:
1. Ορίστε `MAIL_MAILER=smtp` στο `.env`
2. Συμπληρώστε `MAIL_HOST`, `MAIL_PORT`, `MAIL_USERNAME`, `MAIL_PASSWORD`
3. Ή χρησιμοποιήστε `MAIL_MAILER=ses` με διαπιστευτήρια AWS SES.

### Ενεργοποίηση χρονοδιαγράμματος (για production)

Προσθέστε αυτό στο crontab του server σας:
```
* * * * * cd /path/to/client-manager && php artisan schedule:run >> /dev/null 2>&1
```

Τοπικά στο Laragon, μπορείτε να το δοκιμάσετε εκτελώντας:
```bash
php artisan schedule:work
```

---

## Λίστα Ελέγχου Δοκιμών

### Σύνδεση
- [ ] Επίσκεψη στο `/` → ανακατεύθυνση στο `/login`
- [ ] Λάθος στοιχεία → εμφάνιση σφάλματος, παραμονή στη σελίδα σύνδεσης
- [ ] Σωστά στοιχεία → μετάβαση στην Αρχική
- [ ] "Να με θυμάσαι" → η session διατηρείται μετά επανεκκίνηση browser
- [ ] Αποσύνδεση → ανακατεύθυνση στο `/login`, το back-button δεν επιστρέφει

### Πελάτες
- [ ] Προσθήκη πελάτη (μόνο όνομα) → εμφανίζεται στη λίστα
- [ ] Προσθήκη πελάτη (όλα τα πεδία) → αποθηκεύονται σωστά
- [ ] Αναζήτηση με όνομα, email, υπεύθυνο — αποτελέσματα φιλτράρονται άμεσα
- [ ] Επεξεργασία πελάτη → αλλαγές αποθηκεύονται
- [ ] Διαγραφή πελάτη → αφαιρείται ο πελάτης και όλα τα δεδομένα του

### Projects
- [ ] Προσθήκη project από σελίδα πελάτη → εμφανίζεται κάτω από τον συγκεκριμένο πελάτη
- [ ] Επεξεργασία project → αλλαγή ημερομηνίας ανανέωσης αντικατοπτρίζεται στη λογική υπενθυμίσεων
- [ ] Διαγραφή project → αφαιρείται το project, χρεώσεις που σχετίζονται εμφανίζουν `—` για project

### Χρεώσεις
- [ ] Προσθήκη χρέωσης με ημερομηνία λήξης στο παρελθόν → εμφανίζεται στην καρτέλα Ληξιπρόθεσμες, κόκκινη επισήμανση
- [ ] Καταγραφή μερικής πληρωμής → κατάσταση γίνεται Μερικώς Εξοφλημένη
- [ ] Καταγραφή υπόλοιπης πληρωμής → κατάσταση γίνεται Εξοφλημένη, εξαφανίζεται από καρτέλα Ανεξόφλητες
- [ ] Επεξεργασία ποσού πληρωμής → κατάσταση επανυπολογίζεται
- [ ] Διαγραφή πληρωμής → κατάσταση επανυπολογίζεται
- [ ] Αναζήτηση με τίτλο και με όνομα πελάτη

### Προσφορές
- [ ] Προσθήκη προσφοράς με 3 γραμμές → σύνολο υπολογίζεται σωστά
- [ ] Δυναμική προσθήκη/αφαίρεση γραμμών
- [ ] Επεξεργασία προσφοράς → γραμμές ενημερώνονται (σειρά διατηρείται)
- [ ] Αλλαγή κατάστασης σε Απεσταλμένη → ορισμός ημερομηνίας αποστολής, εμφανίζεται στην καρτέλα Απεσταλμένες
- [ ] Αλλαγή κατάστασης σε Αποδεκτή/Απορριφθείσα → ετικέτα ενημερώνεται
- [ ] Λήψη PDF → αρχείο κατεβαίνει με σωστό όνομα και περιεχόμενο

### Υπενθυμίσεις
- [ ] Εκτέλεση `php artisan reminders:overdue` → έλεγχος `storage/logs/laravel.log`
- [ ] Εκτέλεση `php artisan reminders:renewals --days=365` (ευρύ παράθυρο για να εντοπιστούν όλα τα projects) → έλεγχος log
- [ ] Εγγραφή εμφανίζεται στο Αρχείο Email στο `/email-logs`

---

## Προτεινόμενες Λειτουργίες (Μόνο Κρίσιμες)

Αυτές ήταν σκόπιμα εκτός πεδίου εφαρμογής, αλλά αξίζει να εξεταστούν μόλις η εφαρμογή μπει σε τακτική χρήση:

### 1. ~~Εξαγωγή PDF προσφοράς~~ ✓ Υλοποιήθηκε
Κουμπί "↓ Download PDF" στη σελίδα επεξεργασίας κάθε προσφοράς.

### 2. Δημιουργία τιμολογίου / απόδειξης
Προς το παρόν οι χρεώσεις είναι αποκλειστικά εσωτερικές. Ένα απλό εκτυπώσιμο τιμολόγιο (PDF) ανά χρέωση θα επέτρεπε την αποστολή επίσημου εγγράφου στον πελάτη.

### 3. Σημειώσεις / ιστορικό ενεργειών ανά πελάτη
Ένα πεδίο ελεύθερου κειμένου (ή απλό χρονολόγιο γεγονότων) στη σελίδα λεπτομερειών πελάτη. Χρήσιμο για καταγραφή κλήσεων, συμφωνιών και προσωπικού πλαισίου.

### 4. Soft deletes (μαλακή διαγραφή)
Προς το παρόν οι διαγραφές είναι οριστικές (με cascade). Η προσθήκη `SoftDeletes` στα μοντέλα `Client`, `Charge` και `Offer` επιτρέπει την ανάκτηση εγγραφών που διαγράφηκαν κατά λάθος.

### 5. Κλωνοποίηση προσφοράς
Ένα γρήγορο κουμπί "Αντιγραφή" σε μια προσφορά για αντιγραφή των γραμμών της σε νέο πρόχειρο. Εξοικονομεί χρόνο όταν οι προσφορές είναι παρόμοιες μεταξύ πελατών.

### 6. Ομαδοποίηση ανεξόφλητων χρεώσεων στην Αρχική
Ομαδοποίηση ανεξόφλητων χρεώσεων ανά ηλικία: 0–30 ημέρες, 31–60, 61–90, 90+. Δίνει άμεση εικόνα των απαιτήσεων.

### 7. Εξαγωγή CSV
Κουμπί "Εξαγωγή CSV" στη λίστα χρεώσεων με βάση την τρέχουσα προβολή. Χρήσιμο για λογιστές ή ετήσια αναφορά.
